import { getProvinceTrackingDefaults } from "lib/stationData";

export const PROVINCE_TRACKING_DEFAULT_STATIONS = getProvinceTrackingDefaults();

export const PROVINCE_TRACKING_TEMP_TO_TRACK = {
  MIN_TEMP: "min",
  MAX_TEMP: "max",
};

export const PROVINCE_TRACKING_MAX_STATIONS = 6;
